# cursive generator 网站实现说明

这份说明不是写 SEO，而是把上面的站点结构翻译成代码实现思路，方便后面我或 Claude Code 直接开始搭站。

## 一、目标

代码层面要满足：

- 一个关键词一个页面
- 页面核心内容直接渲染到 HTML
- 不同页面共用同一套工具逻辑
- 不同页面拥有不同的 SEO 文案、示例内容、内链关系

## 二、推荐技术栈

优先推荐：

- `Next.js App Router`
- `TypeScript`
- `Tailwind CSS`

原因：

- 很适合做多落地页结构
- SSR / SSG 方便
- `generateMetadata` 易于管理 SEO

## 三、推荐目录结构

```text
网站代码/
  app/
    page.tsx
    layout.tsx
    sitemap.ts
    robots.ts
    cursive-text-generator/page.tsx
    cursive-font-generator/page.tsx
    cursive-signature-generator/page.tsx
    cursive-name-generator/page.tsx
    text-to-cursive-generator/page.tsx
    cursive-text-generator-copy-and-paste/page.tsx
    cursive-handwriting-name-generator/page.tsx
    cursive-alphabet-generator/page.tsx
  components/
    seo/
    layout/
    generator/
    sections/
  content/
    pages.ts
  lib/
    generator.ts
    seo.ts
    links.ts
```

## 四、内容管理建议

不要把每个页面都手写一套重复 JSX。

更好的做法是：

- 用 `content/pages.ts` 管理页面文案配置
- 每个页面只传一个 slug
- 页面模板共用

例如每页配置这些字段：

- `slug`
- `title`
- `description`
- `h1`
- `heroTitle`
- `heroDescription`
- `examples`
- `faq`
- `relatedLinks`

## 五、生成器逻辑建议

工具核心逻辑建议抽到：

- `lib/generator.ts`

它负责：

- 输入普通文本
- 输出多种 cursive 样式
- 为结果生成 label

页面层只负责：

- 展示
- 文案
- 示例
- 内链

## 六、组件建议

### 1. `GeneratorPanel`

负责：

- 输入框
- 实时结果展示
- 一键复制

### 2. `HeroSection`

负责：

- H1
- 副标题
- 工具首屏承接

### 3. `ExamplesSection`

负责：

- 名字示例
- 签名示例
- 平台使用示例

### 4. `FaqSection`

负责：

- FAQ 文字输出
- 帮助搜索引擎理解页面

### 5. `RelatedLinksSection`

负责：

- 相关页面内链
- 举全站之力优化

## 七、SEO 实现注意事项

### 每页都要有

- 唯一 `title`
- 唯一 `description`
- 唯一 `H1`
- canonical
- 可抓取正文

### 不建议

- 用纯客户端渲染首屏正文
- 用 JS 异步再补核心介绍文字
- 所有页面共享同一套 metadata

## 八、后续可以加的能力

第一版不用太复杂，但后续可加：

- 预设示例库
- 热门结果展示
- 本地收藏
- 最近使用
- 精选名字/签名词条

如果未来要继续贴哥飞的精品工具页思路，还可以加：

- 精选结果展示区
- 用户生成内容白名单聚合
- noindex 的用户结果页

## 九、开发优先级

### 第一阶段

- 首页
- 5 个核心功能页
- 2 个场景页
- 公共 SEO 模板
- sitemap / robots / canonical

### 第二阶段

- alphabet 页
- 更多示例内容
- 更细的内链推荐

## 十、总结

这套站最适合的实现方式不是“写 8 套完全不同的页面”，而是：

**一套工具内核 + 一套页面模板 + 多份关键词落地页配置。**

这样最符合哥飞“一个关键词一个页面”的方法论，也最适合持续扩页。
